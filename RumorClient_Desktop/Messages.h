#pragma once

/*
Contains logic for the panel that displays message in the current chat
*/

#include <wx/wx.h>

class Messages : public wxPanel
{
public:
	// constructor
	Messages(wxWindow* parent);

	~Messages();


};

